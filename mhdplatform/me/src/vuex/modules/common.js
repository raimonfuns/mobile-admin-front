import {
  OPENALERT
} from '../mutation-types'

// initial state
const state = {
  count: 0
}

// mutations
const mutations = {
  [OPENALERT] (state, message) {

  }
}

export default {
  state,
  mutations
}
