import {

} from '../mutation-types'

// initial state
const state = {

}

// mutations
const mutations = {
  // [OPENUPLOAD] (state, message) {
  //   state.showToggle.showUploadModal = true;
  // }
}

export default {
  state,
  mutations
}
